- building twice in a row fails
  https://github.com/cockpit-project/cockpit/issues/3165
- PAM integration missing (tools/cockpit.pam), review PAM modules
- test-suite fails in a pbuilder chroot
- proper Debian branding
- git builds (pulling deps via npm over the net) vs minified js in the dist
  tarball
- double-check package dependencies
- package storaged
  https://github.com/cockpit-project/storaged
- /var/lib/cockpit
- run cockpit-ws unpriviliged under cockpit-ws user
- generate proper upstream changelog from
  https://github.com/cockpit-project/cockpit/releases
- PolicyKit integration, cockpit-polkit needs to be setuid-root
  Add postinst to chmod 4755 /usr/lib/cockpit/cockpit-polkit.
- Update/Review debian/copyright (e.g. for external resources)
- Intra-package dependencies (components should depend on a strict version of
  cockpit-ws)
